虚拟文件系统

vfs 四大对象

Super_Block: 超级块。磁盘中的超级块，用于存储一个具体文件系统的基本信息。
					内存中的Super_block，则是一个包含了一个具体文件系统的数据以及相关操作函数的对象。

inode: 索引节点。存放文件系统中各种文件（文件，目录，软连接，设备）的元数据。每个文件/目录都对应一个inode。
				存放在内存中的inode，包含了，文件类型、权限、拥有者、大小、时间戳，块指针等相关信息。
				而存放在磁盘上的inode，同样包含如上信息。
